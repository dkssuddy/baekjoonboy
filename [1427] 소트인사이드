#include <iostream>
#include <algorithm>
#define MODULAR 100007

using namespace std;
int main() {
	char su[11];
	cin >> su;
	int i=0,kp=0;
	
	while(su[kp] !='\0'){
		i++;
		kp++;
	}
	sort(su,su+i);
	for(int k=i-1;k>=0;k--)
			cout <<su[k];
	return 0;
}
