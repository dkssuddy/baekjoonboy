#include <stdio.h>

int main(void) {
	int score[4];
	int ave=0;
	for(int i=0; i<5;i++)
	{
	  scanf("%d", &score[i]);
  	if (score[i]<40)
	  	score[i]=40;
	}
	
	ave = (score[0]+score[1]+score[2]+score[3]+score[4])/5;
	
	printf("%d", ave);
	return 0;
}
