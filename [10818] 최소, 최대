#include <iostream>
using namespace std;

int main() {
	int N,Biggest,Smallest=0;
	cin >> N;
	int Num[N];
	for (int i=0; i<N; i++)
		cin >> Num[i];
	Biggest = Num[0];
	Smallest = Num[0];
	for(int i=0;i<N; i++)
  {
		if(Num[i]>Biggest)
			Biggest=Num[i];
		if(Num[i]<Smallest)
			Smallest=Num[i];
	}
	cout << Smallest <<' '<< Biggest;
	return 0;
}
