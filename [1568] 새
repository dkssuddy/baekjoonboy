#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int N,count=0;
	cin >> N;
	
	while(N!=0)
		for(int i=1;i<=N;i++)
		{
			count++;
			N -= i;
		}
	cout <<count;
	return 0;
}
