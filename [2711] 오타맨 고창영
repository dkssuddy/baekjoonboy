#include <iostream>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int T;
	cin >> T;
	
	for(int i=0;i<T;i++)
	{
		int where;
		string word;
		cin >> where >> word;
		
		word.erase(where-1,1);
		
		cout  << word<<'\n';		
	}
}
