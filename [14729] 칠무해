#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	int N;
	cin >> N;
	float jumsu[N+1];
	for(int i=0;i<N;i++)
		cin >> jumsu[i];
		
	sort(jumsu,jumsu+N);
	
	cout << fixed;
	cout.precision(3);
	
	for(int i=0;i<7;i++)
		cout << jumsu[i] << "\n";
		
}
