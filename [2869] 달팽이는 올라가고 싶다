#include <iostream>
using namespace std;

int main() {
	int A,B,V,gh;
	cin >> A >> B >> V;
	if(A>V || V==A){
		cout << 1;
		return 0;
	}
	if((V-A)%(A-B)==0)
	  cout << (V-A)/(A-B)+1;
	else
		cout << (V-A)/(A-B)+2;
	return 0;
}
