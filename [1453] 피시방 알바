#include <iostream>
using namespace std;

int cache[101]={0, };

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int N,count=0;
	cin >> N;
	
	for(int i=0;i<N;i++)
	{
		int com;
		cin >> com;
	if (cache[com] == 1 ) count++;
	  cache[com]=1;
	}
	cout << count;
	return 0;
	
}
