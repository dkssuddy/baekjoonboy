#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int X,Y;
	cin >> X >> Y;
	if(X>0 && Y>0)
		cout << "1";
	else if(X<0 && Y>0)
		cout << "2";
	else if(X>0 && Y<0)
		cout << "4";
	else
		cout <<"3";
	return 0;
}
