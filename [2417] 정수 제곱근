#include <iostream>
#include <cmath>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	unsigned long long int a;
	cin >> a;
	unsigned long long int b=sqrt(a);

	cout << (b*b==a?b:b+1);
	return 0;	
}
