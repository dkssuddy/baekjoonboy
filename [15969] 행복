#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int N,max=0,min=1001;
	cin >> N;
	while(N--)
	{
		int a;
		cin >> a;
		if(a>max)
			max=a;
		if(a<min)
			min=a;
	}
	cout << max-min;
	return 0;
}
