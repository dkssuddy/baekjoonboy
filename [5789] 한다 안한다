#include <iostream>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	int n;
	cin >> n;
	
	for(int i=0;i<n;i++)
	{
		string name;	
		cin >> name;
		
		int p=name.length()/2;		
		cout << (name[p-1] == name[p]?"Do-it\n":"Do-it-Not\n");
	}
	return 0;
}
