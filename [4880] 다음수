#include <iostream>
using namespace std;

int main()
{
	for(;;)
	{
		double a,b,c;
		cin >> a>> b>> c;
		if(a==0 && b==0 && c==0)
			break;
		if(c-b==b-a)
			cout << "AP " <<c+b-a<<endl;
		if(c/b==b/a)
			cout << "GP " << c*b/a<<endl;
	}
	return 0;
}
