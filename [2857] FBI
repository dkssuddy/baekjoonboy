#include <iostream>
#include <string>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int count=0;
	
	for(int i=1;i<6;i++)
	{
		string name;
		cin >> name;
		if((int)name.find("FBI") != -1)
			{
				cout << i<<" ";
				count++;
			}		
	}
	if(count==0)
		cout << "HE GOT AWAY!";
	return 0;
	
}
