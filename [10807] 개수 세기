#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int N,v,count=0,num[101];
	cin >> N;
	for(int i=0;i<N;i++)
		cin >> num[i];
	cin >> v;
	for(int i=0;i<N;i++)
		if(num[i]==v)
			count++;
	cout << count;
	
	return 0;
}
